{
  "name": "<%= packageName %>",
  "version": "0.0.0",
  "description": "An easy way to start a Vue project",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    <% package.dependencies.forEach(function(package){ %>
        "<%=package.name%>": "<%=package.version%>",
    <% }) %>
  },
  "devDependencies": {
    <% package.devDependencies.forEach(function(package){ %>
       "<%=package.name%>": "<%=package.version%>",
    <% }) %>
  }
}
